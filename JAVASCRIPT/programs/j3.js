// find() method: it returns the descendant elements of the specified elements

var c2=$("div").find("span");
c2;

// children method

//children() method: it returns all direct children of the specified element

var b1=$("div").children();
b1;

//parent() method : it returns the direct parent of the specified element
var v2=$("span").parent();

//parents() method: it returns all the ancestors elements of the specified elements

var b3=$("span").parents();
b3;
